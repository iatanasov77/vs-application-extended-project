<div id="header">
    <div class="container">
        <div id="welcomeLine" class="row">
        {% if app.user %}
            <div class="span6">Welcome!<strong> {{ app.user.info.fullName }}</strong></div>
        {% else %}
            <div class="span6">&nbsp;</div>
        {% endif %}
            <div class="span6">
                <div class="pull-right">
                    {{ vs_widget_render( 'application-layout', ['application-menu-locales'] ) }}
                    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                    
                    {% set shoppingCartLength = shoppingCart.items | length %}
                    <a href="{% if shoppingCartLength %}{{ path( 'vs_payment_shopping_cart_index' ) }}{% else %}javascript:void(0);{% endif %}">
                        <span class="btn btn-mini btn-primary"><i class="icon-shopping-cart icon-white"></i> 
                        [ {{ shoppingCartLength }} ] Itemes in your cart </span>
                    </a> 
                </div>
            </div>
        </div>
        <!-- Navbar ================================================== -->
        <div id="logoArea" class="navbar">
            <a id="smallScreen" data-target="#topMenu" data-toggle="collapse" class="btn btn-navbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <div class="navbar-inner">
                <a class="brand" href="{{ path( 'app_home' ) }}"><img src="{{ asset('/build/bootstrap-shop-theme/images/logo.png') }}" alt="Bootsshop"/></a>
                
                {{ form_start(searchProductForm, { 'attr': {'class': 'form-inline navbar-search'} }) }}
                    {{ form_widget( searchProductForm.search, {'attr': {'class': 'srchTxt'}} ) }}
                    {{ form_widget( searchProductForm.category, {'attr': {'class': 'srchTxt'}} ) }}
                    {{ form_widget( searchProductForm.submit, {'attr': {'class': 'btn btn-primary'}} ) }}
                {{ form_end(searchProductForm, {'render_rest': false}) }}
                
                {#
                <form class="form-inline navbar-search" method="post" action="products.html" >
                    <input id="srchFld" class="srchTxt" type="text" />
                    <select class="srchTxt">
                        <option>All</option>
                        <option>CLOTHES </option>
                        <option>FOOD AND BEVERAGES </option>
                        <option>HEALTH & BEAUTY </option>
                        <option>SPORTS & LEISURE </option>
                        <option>BOOKS & ENTERTAINMENTS </option>
                    </select> 
                    <button type="submit" id="submitButton" class="btn btn-primary">Go</button>
                </form>
                #}
                
                <ul id="topMenu" class="nav pull-right">
                    {{ knp_menu_render('mainApplication', {'template': 'Menu/main-menu.html.twig'}) }}
                    
                    {% if app.user %}
                        <a href="{{ path( 'app_logout' ) }}" role="button" style="padding-right:0"><span class="btn btn-large btn-success">Logout</span></a>
                    {% else %}
                        <a href="#login" role="button" data-toggle="modal" style="padding-right:0"><span class="btn btn-large btn-success">Login</span></a>
                        {% include 'Modal/login-form.html.twig' %}
                    {% endif %}
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
<!-- Header End====================================================================== -->